<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smart_daily_tasks">
    <!-- Permissions for biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.FINGERPRINT"/>
    
    <!-- Required for file management -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.PREVENT_SCREEN_CAPTURE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />

    <!-- Health Connect natively does not require ACTIVITY_RECOGNITION -->
    
    <!-- Location Permissions (Required for some legacy Huawei/Samsung health sensors) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Health Connect: READ Permissions (System Level) -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_EXERCISE"/>
    
    <!-- Health Connect: WRITE Permissions (System Level) -->
    <uses-permission android:name="android.permission.health.WRITE_STEPS"/>
    <uses-permission android:name="android.permission.health.WRITE_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.WRITE_TOTAL_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE"/>

    <!-- ✅ Expert Legacy Stack: Required for 'health' plugin internal validation check -->
    <uses-permission android:name="androidx.health.permission.READ_STEPS"/>
    <uses-permission android:name="androidx.health.permission.WRITE_STEPS"/>
    <uses-permission android:name="androidx.health.permission.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="androidx.health.permission.WRITE_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="androidx.health.permission.READ_EXERCISE"/>
    <uses-permission android:name="androidx.health.permission.WRITE_EXERCISE"/>

    <!-- GMS Fallback for older hardware/simulators -->
    <uses-permission android:name="com.google.android.gms.permission.health.READ_STEPS"/>
    <uses-permission android:name="com.google.android.gms.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="com.google.android.gms.permission.health.READ_TOTAL_CALORIES_BURNED"/>
    <uses-permission android:name="com.google.android.gms.permission.health.WRITE_STEPS"/>
    <uses-permission android:name="com.google.android.gms.permission.health.WRITE_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="com.google.android.gms.permission.health.WRITE_TOTAL_CALORIES_BURNED"/>

    <application
        android:label="smart_daily_tasks"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name="com.example.smart_daily_tasks.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Health Connect Intent Filter -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
        
        <!-- ✅ Highest Google Standards: Activity Alias for Android 14+ Health Privacy Dashboard -->
        <activity-alias
            android:name="com.example.smart_daily_tasks.ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity="com.example.smart_daily_tasks.MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- ✅ Flutter Local Notifications: Scheduled Reminders Receiver -->
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" android:exported="false" />
        
        <!-- ✅ Flutter Local Notifications: Restore Reminders on Boot (Android 16 DirectBoot Aware) -->
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver" android:exported="false" android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Health Connect Rationale Property (Required for Android 14/SDK 34) -->
        <!-- Health Connect Rationale Description (Dual-Stack for Android 16/Samsung/One UI) -->
        <property
            android:name="androidx.health.PROPERTY_HEALTH_CONNECT_PERMISSION_RATIONALE_ACTIVITY"
            android:value="com.example.smart_daily_tasks.MainActivity" />

        <meta-data
            android:name="health_permissions_rationale"
            android:resource="@string/health_permissions_rationale" />

        <!-- ✅ Crucial: Health Connect Permission Mapping (Fixes "Required Update" status) -->
        <meta-data android:name="com.google.android.health.permission.READ_STEPS" android:value="true" />
        <meta-data android:name="com.google.android.health.permission.READ_ACTIVE_CALORIES_BURNED" android:value="true" />
        <meta-data android:name="com.google.android.health.permission.READ_TOTAL_CALORIES_BURNED" android:value="true" />
    </application>

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <package android:name="com.android.healthconnect.controller" />
        <package android:name="com.sec.android.app.shealth" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/pdf" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
